# 更新日志

## [Unreleased]

## [1.2.0] - 2026-02-14

### 新增功能
- **B 站长视频转录优化**:
  - 全面支持**长视频转录**（无时长限制），自动将长音频切分为 55 秒的小段进行并行/串行处理。
  - **统一 ASR 服务**：废弃了不稳定的阿里云 ASR 实现，统一接入 `asrService`，支持多后端自动降级（Faster Whisper -> 百度 ASR）。
  - **环境自动化**：新增 `scripts/setup-video-tools.sh` 脚本，自动检测并安装 `ffmpeg` 和 `faster-whisper` 环境。

### 修复
- **ffmpeg 路径检测**: 优化了 `getFfmpegPath` 逻辑，优先使用系统 PATH 中的 `ffmpeg` 命令，解决了服务器上路径不匹配导致分割失败的问题。
- **音频分割**: 修复了服务器上 `ffprobe` 探测失败的问题，通过显式传入 B 站 API 返回的视频时长来绕过探测步骤。

## [1.1.0] - 2026-02-14

### 新增功能
- **播客转录优化**: 彻底解决了长播客转录失败的问题，支持任意时长的音频转录。
  - 支持 **M4A (AAC)** 格式，兼容性更好，文件体积更小。
  - 智能分段：将音频切分为 55 秒的小段，完美适配百度 ASR 标准版接口的 60 秒限制。
  - 自动降级：当 ffmpeg 不可用时，尝试使用备用方案。

### 修复
- **百度 ASR 兼容性**: 修复了 WAV 格式文件过大和 PCM 格式无头信息导致识别率低的问题。
- **音频分割**: 修复了 ffmpeg 分割参数，确保输出音频格式正确。
- **URL 去重**: 修复了 URL 去重逻辑，确保同一链接在短时间内不会重复处理（之前为了测试临时禁用了）。

### 配置
- 新增 `FFMPEG_PATH` 环境变量，支持自定义 ffmpeg 路径。
